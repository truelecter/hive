name: Build nas
on:
  push:
  workflow_dispatch:

jobs:
  call-workflow-passing-data:
    uses: ./.github/base-workflows/build-x86-host.yaml
    with:
      configuration: nixos-nas
    secrets: inherit
